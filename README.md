# Room Temperature & Weather Comparison

A small IoT + software project comparing indoor temperature across rooms
against outdoor weather, using ESP32 sensor nodes and a hosted database.

## Status
In progress — node 1 build underway.

## Architecture
See `sensor-project-summary.md` for the full architecture, hardware plan,
and rationale.

## Decisions
See `decisions/` for ADRs documenting key design choices along the way.

## Setup
## - Parts
| Item                                   | Qty | Search Term in Amazon         | Approx cost |
| -------------------------------------- | --- | ----------------------------- | ----------- |
| ESP32 dev board (pre-soldered headers) | 1   | ESP32 dev board with headers  | £11         |
| BME280 sensor breakout (pre-soldered)  | 1   | BME280 breakout pre-soldered  | £6          |
| Female-to-female jumper wires (pack)   | 1   | female to female jumper wires | £4          |

## - Build Steps
Build steps were generated by claude, following instructions (particularly those generated by AI) doesnt always go to plan, so rather than uploading the steps i *should* take, i am uploading the steps i actually took in a build journey file (`build-journey.md`)

You'll need a `config.py` file (not committed — see `.gitignore`) containing:

​```python
WIFI_SSID = "your-network-name"
WIFI_PASSWORD = "your-password"
SUPABASE_URL = "your-supabase-url"
SUPABASE_KEY = "your-supabase-api-key"
​```